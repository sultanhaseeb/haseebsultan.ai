---
import { articles } from '../data/site';
import Icon from './Icon.astro';
---
<section id="writing" class="section writing" aria-labelledby="writing-title">
  <div class="section-heading">
    <div><p class="eyebrow">Notes from the workbench</p><h2 id="writing-title">Featured Writing</h2></div>
    <span class="section-note">Ideas taking shape</span>
  </div>
  <div class="article-list">
    {articles.map((article, index) => (
      <article class="article-row">
        <span class="article-number" aria-hidden="true">0{index + 1}</span>
        <div class="article-content">
          <div class="article-meta flex items-center gap-3"><span>{article.category}</span><span class="meta-separator" aria-hidden="true">/</span><span class="coming-soon">Coming soon</span></div>
          <h3>{article.title}</h3>
          <p>{article.description}</p>
        </div>
        <span class="article-action" role="link" aria-disabled="true" aria-label={`Read Article: ${article.title} (coming soon)`}>Read Article <Icon name="arrow" size={17} /></span>
      </article>
    ))}
  </div>
</section>
